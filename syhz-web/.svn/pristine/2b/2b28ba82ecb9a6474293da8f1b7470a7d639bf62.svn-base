$(function() {
	// 切换导航状态
	toggleNav(2, 0, 0);
	/*$("#deliverOrderCommodityGiftTable").DataTable({
		lengthChange : false,
		ordering : false,
		searching : false
	});*/
	$("#giftChoosingTable").DataTable({
		lengthChange : false,
		ordering : false,
		searching : false
	});
});

/**
 * 打开赠品选择窗口
 */
function openGiftChoosingModal() {
	$("#giftChoosingModal").modal("open");
}

/**
 * 打开赠品编辑窗口
 */
function openGiftEditingModal() {
	$("#giftEditingModal").modal("open");
}